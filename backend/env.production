# Production Environment Variables
# Update these values in your deployment platform

# MongoDB Connection String (use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/job-tracker

# Server Port (will be set by deployment platform)
PORT=10000

# JWT Secret (generate a strong random string)
JWT_SECRET=your-production-jwt-secret-here

# Environment
NODE_ENV=production
